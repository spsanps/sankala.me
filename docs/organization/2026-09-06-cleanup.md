# Folder cleanup — September 6, 2026

San resumed the previously paused organization work after the first film's
publishing walkthrough and X-copy discussion. This pass covers the website,
related Paper Robots production repo and media studio.

## Start here next time

- [Website README](../../README.md): the project map and links to film production.
- [Online presence plan](../online-presence-plan.md): decisions, preferences,
  launch status and unresolved work.
- [Branding index](../../branding/README.md): prepared banner/profile uploads.
- The current film release's `UPLOAD.md`, `RELEASE.md` and
  `social/x/launch-quote-post-draft.md`: upload guide, publication record and copy.

## Moved material

All paths below are relative to the website repo.

| Earlier home | Current home |
| --- | --- |
| `tmp/sankala_media_brand_website_substack_playbook.md` | `docs/archive/2026-08-15-media-brand-playbook.md` |
| `tmp/proto/` HTML and image folders | `design/prototypes/2026-08-website/` |
| Screenshots and comparisons at the top of `tmp/proto/` | `design/prototypes/2026-08-website/previews/` |
| `tmp/proto/shot.mjs`, `riso.py` | `design/prototypes/2026-08-website/scripts/capture-preview.mjs`, `risograph-preview.py` |
| Original asset commands, generation log and Python cache | `design/prototypes/2026-08-website/history/` |
| `social-assets/bee-sim-social.mp4` and `.webm` | `social-assets/bee-simulation/` |

[The move manifest](2026-09-06-file-moves.json) lists all 98 files, original and
new paths, byte sizes and SHA-256 checksums. Every file matched immediately after
moving. The screenshot helper then received one documented edit: replace the
absolute Playwright import with `import { chromium } from 'playwright'`.

The strategy and prototypes retain their previous Git-ignored status; the
existing `social-assets/` ignore covers its new subfolder. The original strategy,
HTML, images and video exports retain their contents. Only empty old directories
were removed. Existing `.gitignore` entries were preserved and two destination
rules appended.

To reverse a move, consult the manifest and move `to` back to `from` after
checking the old location is still empty. Update the indexes and ignore rules
along with any reversal. The manifest preserves the screenshot helper's original
hash and records its later import edit separately.

## Documentation and continuity

- Added the website, design, historical-strategy and writing-drafts indexes.
- Added `channel/README.md` in the studio, linking shared outro/voice/lettering
  to the website's banner/profile assets without duplicating source artwork.
- Updated film README/CLAUDE instructions to the existing numbered media stages
  and release-specific title/thumbnail files.
- Updated release status: San said YouTube playback looks good. Public status,
  URL, individual settings and A/B activation remain unconfirmed.
- Saved the X quote-post draft with its pending source links and the distinction
  between proposed copy and a verified robotics claim.
- Recorded San's preference for minimal social-copy edits in his own voice.

The film code, numbered media folders, model weights, takes, references and
archives were already organized by purpose. Their runtime paths remain as-is.
Earlier README concerns about the old `sankala` layout had been resolved by an
external September 5 reorganization before this pass. Nothing was committed,
uploaded, posted or deployed during this cleanup.

## Verification

- All 98 moved files are present at their recorded destinations; original paths
  are absent. Checksums confirm 97 unchanged files and the screenshot helper's
  documented import change. All 98 destinations remain Git-ignored.
- All 44 local Markdown links across the updated entry-point documents resolve.
- Chromium loaded all five prototype pages at 1440px and 390px widths: ten
  successful page loads, no broken images and no JavaScript errors. External
  requests were blocked for this local asset check; remote font availability
  was not tested. The pre-existing About `/resume` navigation placeholder is
  documented in the preview guide.
- `node --check` passes for the relocated screenshot helper.
- `npm run build` passes. Vite reports an older Browserslist dataset and a large
  bundle warning; this organization pass did not change application dependencies
  or runtime code.
- `git diff --check` passes in the website and film code repos.
