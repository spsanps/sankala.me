# Website documents

`resume.pdf` is the canonical, current resume used by the website. Keep this stable filename when replacing it with a newer version so existing links continue to work.

Visitors download it as `Sanjayan_Sreekala_Resume.pdf`, as configured in `src/pages/Resume.jsx`.

Older versions belong in [`archive/resumes`](../../archive/resumes/README.md), not in this public directory.
